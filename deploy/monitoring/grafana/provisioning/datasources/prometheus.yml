apiVersion: 1

datasources:
  - name: Prometheus
    # Фиксированный uid — на него ссылается дашборд из provisioning.
    uid: prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
