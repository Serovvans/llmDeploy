# Скопировать в deploy/.env и заполнить (или запустить scripts/install.sh — он создаст .env
# и сгенерирует секреты). Все переменные, кроме COMPOSE_PROFILES, обязательны: compose
# откажется стартовать с пустым значением.

# Какие профили поднимать помимо базового (caddy, bifrost, vllm): пусто, embeddings,
# monitoring или embeddings,monitoring. Задаётся здесь, чтобы systemd-юнит запускал
# ровно то же, что и ручной `docker compose up -d`.
COMPOSE_PROFILES=

# --- Точка входа (Caddy) ---
# DNS-имя из заявки DevOps; клиентский base_url = https://${LLM_HOSTNAME}/v1.
LLM_HOSTNAME=
# corp — сертификат корпоративного CA: deploy/certs/fullchain.pem (цепочка целиком, иначе
# клиенты без промежуточного CA его не проверят) и deploy/certs/privkey.pem.
# internal — собственный CA Caddy, если корпоративного сертификата нет; корневой
# сертификат раздаётся клиентам (docs/runbook.md). Каталог certs/ в .gitignore.
TLS_MODE=

# --- Основная модель (docs/design.md §3.1, §5, §6) ---
MODEL_ID=Qwen/Qwen3.8-27B-FP8
# 64k — стартовый контекст; больше уменьшает число параллельных запросов (§6).
MAX_MODEL_LEN=65536
# 0.85 оставляет место под эмбеддинги (0.06) и резерв драйвера (§5).
GPU_MEM_UTIL=0.85
# Внутренний ключ между Bifrost и vLLM; клиенты его не видят. Сгенерировать:
# openssl rand -hex 32
VLLM_API_KEY=

# --- Gateway (Bifrost, §3.3) ---
# Admin-auth UI и REST управления ключами (http://127.0.0.1:8080 через SSH-туннель).
BIFROST_ADMIN_USERNAME=
BIFROST_ADMIN_PASSWORD=
# Шифрует ключи в SQLite, в том числе в бэкапах bifrost-data. Задать до первого старта
# и сохранить копию вне ВМ: смена или потеря делает базу нечитаемой.
# Сгенерировать: openssl rand -base64 32
BIFROST_ENCRYPTION_KEY=

# 1 — запрет обращений к Hugging Face после первой загрузки моделей (§8).
HF_HUB_OFFLINE=0

# --- Эмбеддинги, профиль embeddings (§3.6) ---
EMBED_MODEL_ID=Qwen/Qwen3-Embedding-0.6B

# --- Мониторинг, профиль monitoring (§3.5) ---
# Обязателен даже без профиля monitoring: иначе Grafana стартовала бы с admin/admin.
GRAFANA_ADMIN_PASSWORD=
